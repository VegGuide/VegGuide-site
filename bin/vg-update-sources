#!/usr/bin/perl

use strict;
use warnings;

use VegGuide::ExternalVendorSource;


my $sources = VegGuide::ExternalVendorSource->All();

while ( my $source = $sources->next() )
{
    $source->process_feed();
}


