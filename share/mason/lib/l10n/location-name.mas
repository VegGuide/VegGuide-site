<% $location->name | h %>\
% if ( $c && $c->client() && $c->client()->localize_for_location($location) && defined $location->localized_name() ) {
 - <% $location->localized_name() | h %>\
% }
<%args>
$location
</%args>
<%init>
return if $m->cache_self( expire_in => '1m', key => "$c-" . $location->location_id );
</%init>
