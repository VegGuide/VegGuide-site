<div id="hours">
%   foreach my $desc (@descriptions) {
  <div class="hours-set">
    <label><% $desc->{days} | h %></label>
    <% $desc->{hours}[0] | h %>
%     if ( $desc->{hours}[1] ) {
    ; <% $desc->{hours}[1] | h %>
%     }
  </div>
%   }
</div>

<%args>
@descriptions
</%args>
