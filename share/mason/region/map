<h2>Map of <% $location->name() %> Entries</h2>

<p>
  Showing <% $count %> entr<% $count != 1 ? 'ies' : 'y' %>
</p>

<& /lib/filter-div.mas, location => $location, search => $search, search_uri_method => 'map_uri' &>

<p>
  <a href="<% $search->uri() %>">Return to the entry listing</a>
</p>

<& /lib/google-map.mas, %ARGS &>

<%args>
$location
$search
</%args>

<%init>
my $count = $search->count();
</%init>

<%attr>
load_google_maps => 1
requires_js => 1
</%attr>
